
public class ComplexityNotes
{
    /*
   f(n) = O(g(n)) Big-O for some constant c>0, c*g(n) is an upper bound(worst case)
   f(n) = Omega(g(n)) : (Big-Omega) For some constant c > 0, c*g(n) is a lower bound (best case)
   f(n) = Theta(g(n)) : (Big-Theta) For some constants c1 and c2 (each> 0)
    */
}
